module.exports = {

    database: {
        host:       process.env.HOST || 'localhost',
        user:       process.env.USER || 'root',
        password:   process.env.PASSWORD || 'Maggieestaloca2021',
        database:   process.env.DATABASE_NAME || 'db_challenge_alkemy_js'
    }

}